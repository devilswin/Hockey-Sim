#ifndef TEAMS_H
#define TEAMS_H
#include <ctime>
#include <iostream>
#include <fstream>
#include <string>
#include <random>
#include <Windows.h>
#include <stdio.h>
#include <vector>
#include <cstdlib>
#include <cmath>
#include "Player.h"
#include "Lines.h"
#pragma once
using namespace std;
/*class Teams
{
private:
    string Team_Name;
    int Team_Num_Val;
public:
    vector<vector<Player>> Team_F_Lines(4, vector<Player>(3));
    vector<vector<Player>> Team_D_Lines(3, vector<Player>(2));
    void initial_LinesVector();
    Teams(string,int);
};
*/
#endif // TEAMS_H
